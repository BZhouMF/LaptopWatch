# 代码审查方法论

> 基于 LaptopWatch 项目全量审查的实战经验总结。

---

## 目录

1. [审查前的准备工作](#一审查前的准备工作)
2. [第一遍：架构扫描](#二第一遍架构扫描)
3. [第二遍：数据流追踪](#三第二遍数据流追踪)
4. [第三遍：异常路径审查](#四第三遍异常路径审查)
5. [第四遍：资源管理审查](#五第四遍资源管理审查)
6. [第五遍：安全与配置审查](#六第五遍安全与配置审查)
7. [第六遍：前端专项审查](#七第六遍前端专项审查)
8. [第七遍：测试与运维审查](#八第七遍测试与运维审查)
9. [如何判断严重程度](#九如何判断严重程度)
10. [编写审查报告的原则](#十编写审查报告的原则)
11. [审查清单速查表](#十一审查清单速查表)

---

## 一、审查前的准备工作

### 1.1 建立全局认知

**不要上来就看代码。** 先用 5 分钟建立项目的全局认知：

```bash
# 1. 看目录结构
ls -la              # 顶层文件
find . -name "*.py" | head -50   # 有哪些 Python 模块
find . -name "*.tsx" | head -30  # 有哪些前端页面

# 2. 看 git log 了解项目演进
git log --oneline -20    # 最近在做什么
git diff --stat HEAD~5   # 最近的变更范围

# 3. 看 README 或 CLAUDE.md
# 了解项目的定位、术语、约定
```

**关键问题**：
- 这是什么类型的项目？（Web 服务 / CLI 工具 / 桌面应用 / 库）
- 有哪些运行模式？它们之间是什么关系？
- 谁是用户？（个人 / 团队 / 公开产品）

### 1.2 确定审查目标

审查前先明确你关注什么维度。不同项目侧重不同：

| 项目类型 | 优先关注 |
|---------|---------|
| 新项目首次审查 | 架构设计、模块拆分、扩展性 |
| 准备发布 | 安全、异常处理、边界条件、内存 |
| 性能问题排查 | 数据流、缓存策略、I/O 模式 |
| 接手他人代码 | 代码风格一致性、隐式约定、死代码 |

---

## 二、第一遍：架构扫描

### 2.1 从入口文件开始

每个项目都有入口点。从这里出发，画出模块依赖图。

**Python Web 项目**：
```
app.py  →  注册 Blueprint  →  各蓝图模块
         →  注册 before_request  →  中间件
         →  注册 errorhandler  →  异常处理
         →  __main__: 启动逻辑  →  子进程/端口
```

**前端项目**：
```
main.tsx → App → Router → Layout → Pages → Components → API Client
```

### 2.2 检查模块职责是否清晰

**好的信号**：
- 蓝图/路由按业务域拆分（auth / core / media / file / category）
- 工具函数集中放在 utils/，每个文件职责单一
- 配置集中在一个文件

**坏的信号**（需要标记）：
- 一个文件超过 500 行
- 同一类逻辑散落在多个文件中（如 7 个地方都调 sync_folder）
- 工具函数之间循环引用
- 配置文件包含业务逻辑

### 2.3 识别"隐式约定"

项目中最容易出 bug 的就是那些**不写在代码里但大家都知道**的规则。

**如何发现**：
- 看注释中的 "注意"/"TODO"/"FIXME"/"HACK"
- 看硬编码的数字（端口号、超时时间、缓存大小）
- 看环境变量名和默认值
- 看 git commit message 中解释了"为什么"的条目

**举例**（来自 LaptopWatch）：
```
约定："config.DB_PATH 以 MEDIA_DIR 的 basename 命名数据库"
实际：代码中 DB_PATH 是固定的 laptopwatch.db
→ 冲突 → 标记为潜在 bug
```

### 2.4 架构层面的检查清单

- [ ] 是否存在一个组件承担了过多职责？
- [ ] 关键路径是否有回退策略？（DB 不可用 → 文件系统）
- [ ] 多进程/多线程场景下状态一致性如何保证？
- [ ] 模块间依赖是单向的还是循环的？
- [ ] 配置和环境变量是否集中管理？
- [ ] 有没有重复实现同一功能的代码？

---

## 三、第二遍：数据流追踪

这是整个审查中**最重要的步骤**。选一个最核心的业务场景，从用户操作追到数据存储再追回来。

### 3.1 选一个端到端场景

```
用户点击"加载更多" 
  → 前端发起 GET /media/load_more?offset=28&limit=28
  → 后端路由到 media_api.load_more()
  → ctx._db_load_more(offset, limit, is_random)
  → 查 media 表 / traverse_media()
  → 返回 JSON
  → 前端 MediaGrid 渲染
```

### 3.2 画出数据从哪里来

对每个接口，问三个问题：
1. **数据从哪里读？**（DB / 文件系统 / 缓存 / 第三方 API）
2. **读之前做了什么准备工作？**（同步 / 校验 / 权限检查）
3. **如果读不到会怎样？**（降级 / 报错 / 静默返回空）

### 3.3 沿着数据流找不一致

**关键技巧**：对比"写入"和"读取"两端的前提条件是否一致。

```
写入端（sync_folder）：
  只同步了根目录 1 层

读取端（_db_load_more is_random）：
  查询了整个 media 表：WHERE m.path LIKE media_prefix + '%'

→ 前提条件不一致 → 深层文件夹数据缺失 → BUG
```

### 3.4 检查缓存一致性

**每个缓存层都要问**：
- 什么条件下缓存被写入？
- 什么条件下缓存被失效？
- 如果缓存在但数据源已变化，会出什么问题？

**常见的缓存问题模式**：
```
模式 1: 缓存永不过期
  → 文件改了/mtime 变了 → 缓存还是旧的

模式 2: 缓存无上限
  → 用户浏览越来越多 → 内存无限增长

模式 3: 同一数据多份缓存
  → 一份更新了另一份没更新 → 可见状态不一致

模式 4: 缓存失效条件不精确
  → 只删了 key 本身的缓存 → 父级/聚合缓存未清理
```

### 3.5 数据流检查清单

- [ ] 写入端和读取端的前提条件一致吗？
- [ ] 缓存与数据源之间的一致性如何保证？
- [ ] 数据是否存在多份副本？各自何时更新？
- [ ] 翻页/分页的数据完整性如何？（翻页过程中新增/删除文件）
- [ ] 随机/排序模式下的数据是否完整？
- [ ] 有几个地方可以修改同一个状态？是否需要锁？

---

## 四、第三遍：异常路径审查

正常路径大家都会写，**区分好代码和健壮代码的是异常路径**。

### 4.1 搜索静默吞异常

**正则搜索**：
```bash
grep -rn "except Exception:" --include="*.py"
grep -rn "except:" --include="*.py"
grep -rn "pass" --include="*.py" | grep -A1 "except"
grep -rn "catch {" --include="*.tsx"
grep -rn "catch.*ignore" --include="*.tsx"
```

**对每一处问**：
1. 这个异常发生的概率是多少？
2. 如果发生了，用户会看到什么？
3. 如果发生了，你能排查出原因吗（日志里有没有记录）？

### 4.2 三级异常处理标准

| 级别 | 场景 | 处理方式 |
|------|------|---------|
| **关键** | DB 不可用、文件系统错误、认证失败 | `logger.error(exc_info=True)` + 返回 JSON `{code: 1, msg}` + 前端显示错误 |
| **重要** | 缩略图生成失败、单个文件读取失败 | `logger.warning` + 降级处理 + 继续运行 |
| **次要** | 非关键缓存 miss、日志记录失败 | `logger.debug` + 静默降级 |

**绝对不能做的**：
- `except Exception: pass` 不加任何日志
- `except: pass` 裸异常捕获
- 前端 `catch { /* ignore */ }` 不给用户任何反馈

### 4.3 检查降级链是否完整

**好的降级链**：
```
DB cover BLOB  →  DB 实时生成  →  文件系统实时生成  →  返回 404 + 错误原因
  (快)            (中等)           (慢)                 (用户知道为什么)
```

**坏的降级链**：
```
DB cover BLOB  →  DB 实时生成  →  文件系统实时生成  →  return '', 404
  (快)            (中等)           (慢)                 (用户只看到白块)
```

### 4.4 异常路径检查清单

- [ ] 每个 `except Exception: pass` 都有充分的理由吗？
- [ ] 关键路径（DB/文件系统/网络）的异常是否被正确记录？
- [ ] 降级链的每一步是否都有日志？
- [ ] 降级的最后一步是否给用户可见的反馈？
- [ ] 异常发生后，系统状态是否还能保持一致？
- [ ] 是否有异常会导致资源泄漏（文件句柄/数据库连接/线程）？

---

## 五、第四遍：资源管理审查

### 5.1 内存管理

**搜索无上限的数据结构**：
```bash
grep -rn "\[.*\] =" --include="*.py"    # 列表
grep -rn "{}" --include="*.py"          # 字典
grep -rn "cache" --include="*.py"       # 缓存相关
grep -rn "useRef\|useState" --include="*.tsx"  # 前端状态
```

**对每个集合问**：
1. 它什么时候增长？
2. 有大小上限吗？
3. 有过期淘汰机制吗？
4. 在极端场景下（100 万文件/1000 次浏览/24 小时运行）会占用多少内存？

### 5.2 存储空间

**检查持久化存储的增长趋势**：
- SQLite DB 在 sync_folder 后会不会无限增长？
- WAL 文件是否定期 checkpoint？
- 日志文件是否轮转？保留多少天？
- 缩略图缓存（DB BLOB / 内存 / 文件系统）各占多少？
- 同一条数据是否被存储了多份？

### 5.3 连接与句柄

**数据库连接**：
- 有没有连接池或 per-request 复用？
- request 结束后连接是否正确关闭？
- 测试环境下连接是否正确隔离？

**文件句柄**：
- 视频流/图片读取后是否正确 close？
- 异常路径下（客户端断连、超时）是否也正确 close？

### 5.4 线程与进程

- daemon 线程的生命周期是否与主进程一致？
- 子进程退出后是否会被正确回收（不会有僵尸进程）？
- 有没有竞态条件（两个线程同时写同一个文件/DB）？

### 5.5 资源管理检查清单

- [ ] 所有缓存集合（dict/list/Map/Set）有上限吗？
- [ ] 同一条数据被存储了多少份？
- [ ] DB WAL 文件有定期 checkpoint 吗？
- [ ] 日志有轮转和保留策略吗？
- [ ] 文件句柄/DB 连接在异常路径下会泄漏吗？
- [ ] 线程/进程的退出路径是否正确？

---

## 六、第五遍：安全与配置审查

### 6.1 硬编码凭据

```bash
grep -rn "password" --include="*.py" | grep -v test
grep -rn "secret" --include="*.py"
grep -rn "token" --include="*.py"
grep -rn "key" --include="*.py"
```

**关键问题**：
- 密码/密钥是硬编码的还是可配置的？
- 是否有默认密码？默认密码是什么？
- Git 历史中是否暴露了真实凭据？

### 6.2 认证与鉴权

- 登录失败有没有限速？
- session 超时时间是否合理？
- 有没有路径穿越保护？（`resolve() + startswith`）
- 有没有 CSRF 保护？（POST 操作是否需要 token）
- before_request 的拦截逻辑是否覆盖了所有路由？

### 6.3 配置管理

**好的配置**：
- 所有可变值集中在 config.py / .env
- 有合理的默认值
- 环境变量命名有前缀（如 LAPTOPWATCH_*）

**坏的配置**：
- 端口号分散在 3 个文件中
- 分页数量前后端不一致
- "临时测试值"留在正式代码中

### 6.4 安全检查清单

- [ ] 有没有硬编码的密码/密钥？
- [ ] HTTP Header 中是否传输了敏感信息？
- [ ] 所有文件系统访问路径是否做了穿越保护？
- [ ] 登录接口有没有频率限制？
- [ ] 服务未激活状态下所有敏感接口都被拦截了吗？
- [ ] 超时设置的默认值合理吗？

---

## 七、第六遍：前端专项审查

### 7.1 API 调用模式

**搜索所有 API 调用**：
```bash
grep -rn "api_client\." --include="*.tsx"
grep -rn "fetch\|axios" --include="*.tsx"
```

**检查点**：
- 每个 API 调用的错误处理是否完整？
- 有没有请求被串行化而没有并行？（可以用 Promise.all 的地方）
- 有没有重复请求没有去重？（快速点击多次）
- 有没有请求没有 abort？（页面切换时取消未完成的请求）

### 7.2 状态管理

**检查点**：
- 状态是存在组件内还是全局？（useState vs Context vs localStorage）
- 有没有"状态分散在 ref + state + localStorage 三处同步"的复杂度？
- 页面切换后状态是否正确重置？
- 浏览器的前进/后退按钮行为是否正确？

### 7.3 性能

**检查点**：
- 列表渲染有没有使用虚拟滚动或分页？
- 图片有没有 `loading="lazy"`？
- 有没有不必要的 re-render？（检查 useEffect 依赖数组）
- 大对象是否被存入了 sessionStorage/localStorage（有 5MB 限制）？

### 7.4 用户体验

**检查点**：
- loading 状态是否所有场景都覆盖了？
- 错误状态是否所有场景都有提示？
- 空状态是否有友好提示？
- 操作失败后 UI 是否"卡住"？
- 有没有静默失败让用户困惑的交互？

### 7.5 前端检查清单

- [ ] 每个 API 调用的 catch 块都有用户可见的错误处理吗？
- [ ] 可以并行发出的请求是否在并行？
- [ ] 页面离开时是否有 abort 未完成的请求？
- [ ] 状态同步是否存在"ref + state + storage"三份拷贝的复杂模式？
- [ ] sessionStorage/localStorage 存储的数据大小是否可控？
- [ ] loading/error/empty 三种状态是否都覆盖了？

---

## 八、第七遍：测试与运维审查

### 8.1 测试覆盖

```bash
# 找到测试文件
find . -name "test_*.py" -o -name "*.test.tsx"

# 运行测试看通过率
python test/run_all.py
```

**关键问题**（不是看覆盖率数字）：
- 测试是否覆盖了**异常路径**？（DB 不可用、文件不存在、网络错误）
- 测试是否用了**真实的数据源**还是全 mock？
  - 全 mock 的测试不会发现 SQL 语法错误和 Schema 不一致
- 有没有**端到端测试**覆盖核心业务场景？
- 测试之间是否完全隔离（不会因为执行顺序而 pass/fail）？

### 8.2 可观测性

**检查点**：
- 日志级别是否可配置？
- 日志格式是否一致？（结构化 vs 文本）
- 关键事件是否有日志？（启动、停止、错误、配置变更）
- 有没有健康检查端点？
- 异常堆栈是否完整记录（`exc_info=True`）？

### 8.3 运维友好度

**检查点**：
- 首次启动需要多少步骤？（`pip install` + `npm install` + `python app.py`）
- 配置文件是否需要手动创建？
- 数据库是否需要手动初始化？
- 有没有打包/部署脚本？
- 更新版本时数据迁移怎么做？

### 8.4 测试运维检查清单

- [ ] 是否有至少一个端到端 smoke test？
- [ ] 测试是否使用独立的数据源（不会污染开发环境）？
- [ ] 测试清理逻辑是否正确（不会残留文件/DB 记录）？
- [ ] CI/CD 流程是否跑全部测试？
- [ ] 有没有健康检查端点？
- [ ] 异常日志是否可以追溯到具体的请求和时间？

---

## 九、如何判断严重程度

### P0 - 阻断发布

满足**任一**条件即为 P0：
- **数据完整性**：用户数据可能丢失、损坏或不可见
- **安全性**：存在可被利用的漏洞（未授权的访问、数据泄露）
- **稳定性**：在正常使用下必定出现的 crash 或不可恢复的错误
- **功能缺失**：核心功能完全不可用

### P1 - 严重影响体验

- **用户体验降级**：功能可用但体验差（如不知道总页数、缩略图加载慢）
- **性能隐患**：正常使用时不会触发，但极限场景下会出问题（如 100 万文件的内存 OOM）
- **维护性差**：代码结构导致后续修改极易引入 bug

### P2 - 打磨优化

- **代码质量**：风格不一致、重复代码、未使用的变量
- **小体验优化**：骨架屏、更好的错误提示
- **配置优化**：默认值不合理、硬编码分散

### 判断框架

```
这个问题的后果是什么？
  ├─ 用户数据丢失/损坏 → P0
  ├─ 用户看到白屏/报错/无提示 → P0（如果高频触发）或 P1
  ├─ 用户永远不知道有这个问题 → P1
  ├─ 开发者以后会踩坑 → P1
  └─ 有更好的写法 → P2
```

---

## 十、编写审查报告的原则

### 10.1 先肯定再批评

每个项目都有做得好的地方。先列出亮点，能让作者更容易接受后面的批评。

### 10.2 每个问题必须包含

```
问题标题（一句话）
文件：具体文件:行号
原因：为什么会发生
影响：用户/开发者会看到什么
复现：（如适用）具体步骤
修复：建议方案
```

### 10.3 区分"问题"和"建议"

- **问题**：会导致错误行为、数据丢失、崩溃 → 标记 P0/P1
- **建议**：不会出错但可以更好 → 标记 P2

### 10.4 提供修复优先级

不要扔一堆问题就走。给一个路线图：
- 第一批：必须现在修的（3-5 个）
- 第二批：下一个迭代修的
- 第三批：有时间再优化

### 10.5 检查你的审查是否完整

自检：
- [ ] 是否追了一条完整的业务场景？（端到端数据流）
- [ ] 是否看了至少一个前端页面和一个后端模块？
- [ ] 是否搜索了 `except.*pass` 和 `catch.*ignore`？
- [ ] 是否检查了缓存策略和内存上限？
- [ ] 是否运行了测试？
- [ ] 是否检查了安全相关（密码、路径穿越、认证）？
- [ ] 是否看了配置文件和环境变量？
- [ ] 是否看了进程/线程管理逻辑？

---

## 十一、审查清单速查表

### 执行顺序（7 遍法）

| 遍数 | 名称 | 耗时 | 核心动作 |
|------|------|------|---------|
| 1 | 架构扫描 | 15min | 读入口文件 → 画模块依赖图 → 识别架构隐患 |
| 2 | 数据流追踪 | 30min | 追一条完整的业务场景 → 对比读写两端 → 检查缓存一致性 |
| 3 | 异常路径 | 20min | grep except/catch → 判断是否静默 → 检查降级链 |
| 4 | 资源管理 | 15min | 搜索缓存/集合 → 检查无上限增长 → 检查连接泄漏 |
| 5 | 安全配置 | 10min | grep password/secret → 检查认证 → 检查路径穿越 |
| 6 | 前端专项 | 15min | 检查 API 调用模式 → 状态管理 → loading/error/empty |
| 7 | 测试运维 | 10min | 运行测试 → 检查可观测性 → 检查部署步骤 |

**总计：约 2 小时完成一次全量审查**

### 关键 grep 搜索模式

```bash
# Python 后端
grep -rn "except Exception:" --include="*.py"
grep -rn "except:" --include="*.py"  
grep -rn "pass" --include="*.py" | grep -B1 "except"
grep -rn "TODO\|FIXME\|HACK\|XXX" --include="*.py"
grep -rn "cache\|Cache" --include="*.py"
grep -rn "password\|secret\|token\|key" --include="*.py" | grep -v test
grep -rn "append\|\.add(" --include="*.py"  # 找无上限的集合增长
grep -rn "threading\|Thread\|Process" --include="*.py"
grep -rn "os\.walk\|os\.scandir" --include="*.py"  # 文件系统扫描
grep -rn "sleep(" --include="*.py"  # 找时间依赖
grep -rn ": int =" --include="*.py"  # 硬编码数字

# TypeScript 前端
grep -rn "catch {" --include="*.tsx"
grep -rn "catch.*ignore" --include="*.tsx"
grep -rn "useState\|useRef" --include="*.tsx" | wc -l  # 状态复杂度
grep -rn "localStorage\|sessionStorage" --include="*.tsx"
grep -rn "api_client\." --include="*.tsx"  # API 调用点
grep -rn "useEffect" --include="*.tsx" | wc -l  # 副作用复杂度
grep -rn "onError" --include="*.tsx"  # 错误处理
```

### 常见代码坏味道速查

| 坏味道 | grep 搜索 | 通常意味着 |
|--------|----------|-----------|
| `except Exception: pass` | `grep -rn "pass" \| grep -B1 except` | 异常被吞，bug 无法追踪 |
| `except: pass` | `grep -rn "^except:"` | 裸异常捕获，连 KeyboardInterrupt 都会吞 |
| `while True: time.sleep(n)` | `grep -rn "while True"` | 轮询循环，应该用事件驱动 |
| `.append(` 在全局变量 | `grep -rn "append"` 然后看作用域 | 内存持续增长无上限 |
| `import` 在函数体内 | `grep -rn "import " \| grep "def "` | 延迟导入，可能是循环引用 |
| `os.path.join` 拼接 URL | `grep -rn "os.path.join.*http"` | URL 和文件路径混用 |
| 注释掉的代码块 | `grep -rn "^#.*def \|^#.*class "` | 死代码残留 |
| `assert` 在非测试代码 | `grep -rn "assert " \| grep -v test` | 生产环境可能被 -O 跳过 |

---

## 附录 A：不同类型的项目审查侧重

### Web 后端
- 侧重：数据流完整性、异常处理、缓存一致性、API 契约、DB Schema

### 前端 SPA
- 侧重：状态管理、API 错误处理、路由守卫、内存泄漏（事件监听器）、打包体积

### CLI 工具
- 侧重：参数校验、stdin/stdout 编码、退出码、信号处理

### 桌面应用
- 侧重：进程生命周期、端口管理、IPC 通信、打包兼容性

### 库/SDK
- 侧重：公共 API 设计、向后兼容性、类型注解、文档完整性

---

## 附录 B：写好代码的元原则

这些是我在审查中反复验证的经验：

1. **显式优于隐式**。不要依赖隐式约定、隐式执行顺序、隐式默认值。写出来。

2. **Crash early, crash loud**。出错了宁可报错让用户知道，也不要在角落里静默吞掉。

3. **数据流比控制流更重要**。想清楚数据从哪里来、经过哪里、最终到哪去。大部分 bug 不是逻辑错了，而是数据不一致。

4. **设计降级，不要设计中断**。DB 不可用时应该能用文件系统，而不是直接报 500。每一步都问"如果这一步失败了，下一步怎么办？"

5. **任何没有上限的集合都是定时炸弹**。缓存、列表、历史记录——只要在运行时可以持续增长，就一定要有淘汰策略。

6. **同一份数据不要有三个缓存层**。每加一层缓存就问自己："这层缓存带来了什么价值？它的失效策略是什么？如果它和另一层不一致会怎样？"

7. **测试异常路径比测试正常路径更有价值**。正常路径在第一次手测时就会发现 bug。异常路径可能在生产环境运行 3 个月后才触发。

8. **代码是写给人看的，顺便能在机器上运行**。变量名、函数名、注释应该让一个不熟悉代码的人在 10 分钟内理解这段代码在做什么。

---

*文档版本：v1.0 | 编写日期：2026-07-01 | 基于 LaptopWatch v0.x 审查实战*
